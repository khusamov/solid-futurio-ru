import ICommand = Game.ICommand;
import DeletableQueue = Base.Data.DeletableQueue;
import UniversalObject = Game.UniversalObject;

const commandQueue: DeletableQueue<ICommand> = new DeletableQueue

const spaceship = new UniversalObject


